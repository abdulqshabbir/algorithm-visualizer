Visualize all the popular sorting algorithms!

Currently only bubble sort and insertion sort are supported.

This project can be run locally by running `npm start` followed by going to localhost:3000.
